# 経理ルール（この会社固有の確定事項。実務の中で判明次第追記する）

## 会計システム

テンプレートはマネーフォワード クラウド会計・freee会計の両方に対応している。`/setup`で使用システムが分かったら、**該当する側のブロックだけを残し、もう一方のブロックとこの案内文は削除する**（CLAUDE.mdの「会計システム接続時の動き」節も同様）。

### 【マネーフォワード クラウド会計の場合】

- 事業者番号：（未設定）／会計期間：（未設定）
- 接続手段：mfc_ca MCP（claude.aiカスタムコネクタ）。1接続＝1事業者のため、接続先の切替はオーナー本人が`/mcp`で操作する（手順は`../.claude/skills/new-instance/SKILL.md`参照）。API直接連携は不可（クラウド会計スコープ非対応）
- 使うスキル：`mf-fee-post`（未仕訳の自動登録。`config/mf-companies.json`にこの会社を追加すること）、`mf-monthly-check`（月次チェック、読み取り専用）
- 勘定科目ID・補助科目ID・税区分IDは会社ごとに別物。毎回`mfc_ca_getAccounts`等で取得する

### 【freee会計の場合】

- company_id：（未設定）／認可状況：（未認可）
- 接続手段：
  - `freee-api-post`スキル（OAuthプライベートアプリ＋自前スクリプト、無人実行向け）。セットアップは`../.claude/skills/freee-api-post/setup-guide.md`。Client ID/Secretの保存先は`.claude/scripts/credentials_freee_<会社名>.json`（`.gitignore`対象。AIは読み書きできないので、オーナー本人が作成する）
  - freee MCPコネクタ（対話中の参照用）。接続時は`freee_get_current_company`で会社名・会社IDを報告する
- 使うスキル：`freee-api-post`（認証・ヘルスチェック）、`freee-monthly-check`（月次チェック、読み取り専用）
- **freee APIには明細の消込機能が無い**（消込・複合仕訳はAPI/MCPどちらも不可）。確立パターンはfreeeの「自動登録ルール」を1回設定して以降はfreee自身に任せ、秘書/APIは設定と監視に徹する。`POST /deals`で仕訳を直接作る運用はしない（`../.claude/rules/output-format.md`「freee会計の自動処理方針」参照）

## 計上パターン
- 売上・費用の計上基準：（未設定・実務の中で判明次第追記）
- 売上の締め日・入金サイト：（未設定）
- 支払の締め日・支払サイト：（未設定）

## 消込確認対象科目
- デフォルト：売掛金・買掛金・未収入金・未払金・仮払金・仮受金（追加があれば追記）

## 自動仕訳の確認済みパターン（`mf-fee-post`／freee自動登録ルールが参照する）

「摘要は同じでも金額は変動、勘定科目は固定」という型で蓄積する。ここに無いパターンは自動登録しない。

| 摘要パターン | 勘定科目／補助科目 | 税区分 | 金額の傾向 | 確認日 |
|---|---|---|---|---|
| （例）振替 ○○テスウリヨウ | 支払手数料 | 課税仕入10% | 固定 | |

## 重複チェックで除外してよい科目の組み合わせ（`mf-monthly-check`の`duplicate_known_safe_pairs`）
- （例）支払手数料／普通預金：多数の異なる振込に対する定額の銀行手数料で、同日同額が正常

## グループ会社間取引
- 該当：（未設定：あり／なし）。ありの場合、相手会社名の補助科目（未収入金・未払金）と表記ゆれを記録する

## 飲食費の会議費／交際費の判定

- 交際費等から除外できる飲食費は**1人当たり税抜1万円以下**（令和6年度税制改正、2024年4月以降）。以下なら会議費、超えれば交際費
- 領収書に人数があれば1人当たりで判定する。人数不明時の目安（例：通常2人の会食なら税抜2万円）はこの会社の実態に合わせてここに書く：
  - （未記入）

## 領収書の既定の支払方法（会計システムを使わない会社の帳簿づくり・kessan）

領収書・請求書を読み取ったとき、支払方法が分からず、口座・カード・現金の明細にも該当が無い場合に使う既定値。ここを決めたら、`kessan-sources.yaml` の `receipt_default` にも同じ値を書く（スクリプトはそちらを読む）。会計システムを使う会社はこの節を削除してよい。

- 既定の支払方法：（未設定。立替＝役員が個人で立て替えて後で精算する（貸方 役員借入金）／現金＝会社の現金で払う（貸方 現金））
- 例外（取引先・内容ごとに支払方法が決まっているもの）：（未設定）

## 月次の経理フロー・承認フロー

（未設定・実務の中で判明次第追記）

### 速報締め→確定締めの2段階運用（該当する会社のみ）

決済代行の精算書・取引先からのデータなど、確定データの到着が遅い会社では、月次PLを一度「速報値」で締め、確定データが届いてから正規の締めをやり直す運用をとることがある。該当しない会社はこの節を「該当なし」のままにしてよい。

- 該当：（未設定。該当あり／なし）
- 速報締めの期限・対象科目：（未設定。例：翌月〇日までに、管理画面の速報値で売上・決済手数料を概算計上）
- 速報値の計上方法：（未設定。概算で計上する仕訳の型、摘要に「速報」等の目印を付けるか、専用の補助科目を使うか）
- 確定締めの手順：（未設定。確定データ到着後、速報仕訳を削除して正規仕訳に差し替えるのか、差額仕訳で調整するのか。差し替え漏れ・二重計上を防ぐため、速報仕訳の仕訳番号を`work/`に記録しておく）
- 確定データの到着時期：（未設定）

## 特記事項
- （未設定・実務の中で判明次第追記）
