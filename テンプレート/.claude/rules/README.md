# このフォルダについて

文体・出力書式・機密の扱いといった全クライアント共通のルールは `keiri-hisho/.claude/rules/` に置いてある（`communication.md` / `output-format.md` / `security.md`）。

このフォルダ（テンプレート固有の `.claude/rules/`）は、共通ルールでは足りない、このテンプレート／個別クライアント固有の追加ルールが出てきたときにファイルを置く場所。まだ追加ルールはない。
